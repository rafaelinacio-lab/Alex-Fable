{
  "name": "movidesk-autonomous-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Agente autônomo de atendimento Movidesk (VIASOFT) — ferramentas tipadas + orquestrador com Claude",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/agent/cli.ts",
    "check-env": "tsx src/config/checkEnv.ts",
    "test": "node --import tsx --test test/**/*.test.ts",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@google/genai": "^2.21.0",
    "exceljs": "^4.4.0",
    "openai": "^7.5.0",
    "pdfkit": "^0.20.1",
    "ws": "^8.21.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/pdfkit": "^0.17.6",
    "@types/ws": "^8.18.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
